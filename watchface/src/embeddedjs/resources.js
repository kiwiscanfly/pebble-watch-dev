// Named handles for the positional resource IDs declared in package.json
// "resources.media" (1-based, in declaration order — keep the two in sync).
export const RESOURCES = {
    ICON: 1,
    BOLT: 2
};
